{
  "name": "regex-matcher",
  "private": true,
  "version": "0.0.2",
  "scripts": {
    "dev": "vite",
    "build": "vite build --emptyOutDir",
    "preview": "vite preview",
    "deploy": "vite build --mode=gh-pages --emptyOutDir && gh-pages -d dist -m Deploy"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "path": "^0.12.7",
    "sass": "^1.58.3",
    "vite": "^4.1.0"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.3"
  }
}
